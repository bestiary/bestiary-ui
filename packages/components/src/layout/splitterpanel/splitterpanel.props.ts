export interface SplitterPanelProps {
    /**
     * Initial size of the panel in percentage.
     * @default undefined
     */
    size?: number;

    /**
     * Minimum size of the panel in percentage.
     * @default 0
     */
    minSize?: number;
}