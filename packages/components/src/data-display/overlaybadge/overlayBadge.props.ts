import { BadgeProps } from "../badge/badge.props";

export interface OverlayBadgeProps extends BadgeProps {
    /**
     * Note: All props are inherited from BadgeProps.
     * This component overlays a badge on top of its content.
     */
}