import type { BadgeProps } from './badge.props';

/**
 * Inherits all props from BadgeProps.
 * Used to position a badge absolutely over its wrapping container.
 */
export type OverlayBadgeProps = BadgeProps;