import Password from "./Password.vue";
import { withInstall } from "../../utils/with-install.ts";

export const BPassword = withInstall(Password);
export default BPassword;

export * from "./password.types";